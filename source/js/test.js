// 'use strict';

// function makeMoreTxt(nodes) {
//   for(let i = 0; i < nodes.length; i++) {
//     console.dir(nodes[i],1);
//     nodes[i].textContent = nodes[i].textContent.repeat(3);
//   }
// };

// let paragraphs = document.querySelectorAll('p');
// makeMoreTxt(paragraphs);

// let links = document.querySelectorAll('a');
// makeMoreTxt(links);

// let spans = document.querySelectorAll('span');
// makeMoreTxt(spans);
